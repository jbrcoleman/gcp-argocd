apiVersion: v1
kind: Service
metadata:
  name: python-service
  namespace: python-service
spec:
  selector:
    app: python-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
